export const qrcode = (state) => {
  return state.qrcode
}

export const rippleAddressData = (state) => {
  return state.rippleAddressData
}

export const derivedAddress = (state) => {
  return state.derivedAddress
}

export const derivedKeypair = (state) => {
  return state.derivedKeypair
}
